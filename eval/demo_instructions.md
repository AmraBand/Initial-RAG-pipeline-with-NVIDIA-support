# Demo Artifact

Add one of the following here for submission:

- A short GIF showing a terminal run of `python retrieve_generate.py --query "..."` with cited chunk IDs in the answer.
- A screenshot of the same output.
- A link to a hosted demo video.

Suggested capture checklist:
1. Show `ingest.py` run summary.
2. Show `index.py` run summary.
3. Show one `retrieve_generate.py` query result with citations and confidence line.
